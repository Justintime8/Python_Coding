hrs = input("Enter Hours:")
h = float(hrs)
ot = input('enter:')
o = float(ot)
rate = input('enter:')
r = float(rate)
otrate = input('enter:')
otr = float(otrate)

if h > 40:
    print( 40 * r + o *(r * otr))
